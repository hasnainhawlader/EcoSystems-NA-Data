<<<<<<< HEAD
name: CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: hasnain-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python 3.12.4
      uses: actions/setup-python@v4
      with:
        python-version: 3.12.4

    - name: Install Dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r project/requirements.txt

    - name: Run Tests
      run: |
        chmod +x project/tests.sh
        ./project/tests.sh
      env:
        API_KEY: ${{ secrets.MADE_NA }}
=======
name: CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: hasnain-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python 3.12.4
      uses: actions/setup-python@v4
      with:
        python-version: 3.12.4

    - name: Install Dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install -r project/requirements.txt

    - name: Run Tests
      run: |
        chmod +x project/tests.sh
        ./project/tests.sh
      env:
        API_KEY: ${{ secrets.MADE_NA }}
>>>>>>> a759c200e901f4f70a4b588697eed6bdcb5b8fc5
